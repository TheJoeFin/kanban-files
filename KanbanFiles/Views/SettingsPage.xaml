<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="KanbanFiles.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:KanbanFiles.ViewModels"
    Background="Transparent"
    mc:Ignorable="d">

    <Page.DataContext>
        <viewmodels:SettingsViewModel x:Name="ViewModel" />
    </Page.DataContext>

    <Grid Padding="24">
        <ScrollViewer>
            <StackPanel MaxWidth="600" Spacing="16">
                <TextBlock
                    Style="{StaticResource TitleTextBlockStyle}"
                    Text="Settings" />

                <!--  Appearance  -->
                <TextBlock
                    Margin="0,8,0,0"
                    Style="{StaticResource SubtitleTextBlockStyle}"
                    Text="Appearance" />

                <StackPanel Spacing="4">
                    <TextBlock
                        Style="{StaticResource BodyStrongTextBlockStyle}"
                        Text="Column width" />
                    <TextBlock
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        Style="{StaticResource CaptionTextBlockStyle}"
                        Text="Set the width of each column on the board." />
                    <StackPanel Orientation="Horizontal" Spacing="12">
                        <Slider
                            Width="300"
                            Maximum="600"
                            Minimum="200"
                            StepFrequency="10"
                            Value="{x:Bind ViewModel.ColumnWidth, Mode=TwoWay}" />
                        <TextBlock
                            VerticalAlignment="Center"
                            Text="{x:Bind ViewModel.ColumnWidth, Mode=OneWay}" />
                    </StackPanel>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
